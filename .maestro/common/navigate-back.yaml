name: Navigate back
appId: ${MAESTRO_APP_ID}
tags:
  - navigation
---
- runFlow:
    when:
      platform: Android
    commands:
      - tapOn:
          id: "topNavBackAction"
- runFlow:
    when:
      platform: iOS
    commands:
      - tapOn:
          text: "Back"
